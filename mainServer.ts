import express from 'express';
import prod from '../week 6 assesment/src/Routes/Prod';
import { join } from 'path/posix';


const app = express();



app.use("", ) 


app.use(express.json());







app.listen(3000, () => {
    console.log('server is running ')
})